# Experiment Folders

Each subfolder in this directory represents one task plus algorithm combination.

Place run-specific outputs next to the report:

- `metrics.json` for scalar results used by `scripts/summarize_results.py`
- `logs/` for trainer outputs
- `videos/` for evaluation rollouts

The folders are intentionally pre-created so you can keep notes and results isolated by combination.